{
  "name": "synexora-backend-orchestrator",
  "version": "0.1.0",
  "private": true,
  "description": "Unified backend runner for Synexora Spring Boot API and FastAPI AI Service",
  "scripts": {
    "dev": "concurrently -n \"API,AI\" -c \"green.bold,cyan.bold\" \"npm run dev:api\" \"npm run dev:ai\"",
    "dev:api": "node scripts/run-api.js",
    "dev:ai": "node scripts/run-ai.js",
    "install:all": "npm install && cd ai-service && (python -m venv venv || python3 -m venv venv) && node ../scripts/install-ai.js"
  },
  "dependencies": {
    "concurrently": "^9.1.0"
  }
}
